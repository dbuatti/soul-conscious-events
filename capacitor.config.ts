import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.soulconsciousevents',
  appName: 'Soul Conscious Events',
  webDir: 'dist'
};

export default config;
